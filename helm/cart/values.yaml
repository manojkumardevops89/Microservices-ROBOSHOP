name: cart

replicaCount: 2

image:
  repository: triallwgji5.jfrog.io/roboshop/cart
  tag: latest

service:
  port: 8080

env:
  - name: REDIS_HOST
    value: redis
hpa:
  minReplicas: 1
  maxReplicas: 5
  cpuThreshold: 70
