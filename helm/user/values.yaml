name: user

replicaCount: 2

image:
  repository: <ECR>/user
  tag: latest

service:
  port: 8080

env:
  - name: MONGO_HOST
    value: mongodb
hpa:
  minReplicas: 1
  maxReplicas: 5
  cpuThreshold: 70
